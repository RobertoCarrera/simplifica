// CSS / asset import mock. Jest tries to parse any import it finds in
// TypeScript files, including `import './foo.scss'`. Returning an empty
// object is enough for the test runner to proceed.
export default {};
