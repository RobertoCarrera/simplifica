import { TranslocoTestingModule } from '@jsverse/transloco';

/**
 * Returns a transloco testing module for use in TestBed imports.
 * Mirrors what the original transloco-testing.module.ts provided.
 */
export function translocoTesting() {
  return TranslocoTestingModule;
}
