# Palette's Journal

## 2024-05-22 - Accessibility in Shared Components
**Learning:** Shared components like icon-only buttons often lack `aria-label`, making them inaccessible to screen readers.
**Action:** Audit and add `aria-label` to all icon-only buttons in `src/app/features` and `src/app/shared/ui`.
