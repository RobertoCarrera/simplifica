# Configuración de hosts para testing multi-tenant local
# Agregar estas líneas a C:\Windows\System32\drivers\etc\hosts (Windows)
# o /etc/hosts (Linux/Mac)

# Desarrollo local - Simular subdominios
127.0.0.1 crm.michinanny.local
127.0.0.1 admin.anscarr.local  
127.0.0.1 panel.liberatuscreencias.local
127.0.0.1 crm.satpcgo.local
127.0.0.1 admin.tudominio.local

# Uso:
# http://crm.michinanny.local:4200 -> Vista de Michinanny
# http://admin.anscarr.local:4200 -> Vista de Anscarr  
# http://admin.tudominio.local:4200 -> Vista Super Admin

# Nota: En producción cada subdominio apuntará a la misma IP 
# del servidor, pero Angular detectará el tenant por el hostname